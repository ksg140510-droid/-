# 빌드 산출물 (소스에서 다시 만들 수 있음 — git에 안 넣음)
build/
dist/
*.exe

# 파이썬 캐시
__pycache__/
*.pyc

# 실행 중 생성되는 로컬 데이터 (혹시 이 폴더 안에 생기더라도 제외)
# 검사 이력(DB)·설정 파일은 실제 생산 데이터를 담을 수 있어 공개 저장소에
# 절대 올리면 안 됨 — 실수로라도 커밋되지 않도록 확실히 제외.
*.json
*.db
*.db-journal
*.db-wal
*.db-shm
